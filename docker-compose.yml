
services:
  chatbot:
    build: .
    ports:
      - "8080:8080"     # localhost:8080 → container:8080
    env_file:
      - .env            # reads GOOGLE_API_KEY from your local .env file
    restart: unless-stopped